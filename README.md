# Marp to Google Slides (GAS)

Markdown (Marp形式)で記載された構成テキストを、Googleスライドの「指定テンプレート」にデザインを崩さずに一括で流し込むための専用Google Apps Script（GAS）ツールです。

## 特徴
1. **Googleスライドのコーポレートデザイン（テンプレート）を完全保持**
   * MarpからPPTXを経由してGoogleスライドにインポートすると、マスターレイアウトが破壊される問題を解決します。
2. **スピーカーノート（台本）への完全対応**
   * Markdown内の HTMLコメント（`<!-- ノート内容 -->`）を自動認識し、スライドのノート領域へ流し込みます（Marp互換）。
   * 旧フォーマットの `<!-- speaker_note -->` も引き続き使用できます。
3. **安全なサイドバーUIによる流し込み**
   * GASのコードエディタに直接Markdownを貼り付けるのではなく、スライドのメニュー画面から呼び出せる専用UI（サイドバー）を提供し、改行コードやバッククォートの構文エラーを完璧に回避します。
4. **「サンプル挿入」ボタンで書き方をその場で確認**
   * サイドバーの「✨ サンプルフォーマットを挿入」ボタンを押すと、正しい書式のサンプルMarkdownが一発で入力欄に展開されます。サンプルを書き換えるだけですぐに使い始められます。

## 導入方法
1. 流し込み先の「空のGoogleスライド」を開きます。
2. 上部メニューから **「拡張機能」>「Apps Script」** を開きます。
3. デフォルトで書かれている `function myFunction() {}` などをすべて消去し、本リポジトリの `Code.gs` の内容をすべて貼り付けて保存（Ctrl+S）します。
4. 元のGoogleスライド画面に戻り、F5キー等で画面をリロード（再読み込み）します。

## 使い方
1. リロード後、上部のメニューバー右端に **「📘 Markdown流し込み」** という独自のメニューが出現するのでクリックします。
2. **「スライド自動生成ツールを開く」** を選択します。
3. 画面右側にサイドバーが出現するので、「✨ サンプルフォーマットを挿入」ボタンでサンプルを確認するか、Marp形式のMarkdownテキストを直接貼り付けます。
4. **「スライドを自動生成する」** ボタンを押すと、数秒〜十数秒で全スライドが生成されます。

## Markdownのフォーマット仕様

このツールは **Marp互換のMarkdown形式** を使用します。

### 基本構造

```markdown
---
marp: true
---

# スライド1のタイトル

本文の内容をここに書きます。

- 箇条書き1
- 箇条書き2

<!-- スピーカーノートはHTMLコメント形式で書きます。 -->

---

# スライド2のタイトル

コンテンツ...

<!-- スライド2のスピーカーノート -->
```

### ルール
| 要素 | 書き方 |
|------|--------|
| スライドの区切り | `---`（前後に空行） |
| スライドタイトル | `# タイトル` |
| 本文 | 通常のMarkdown（箇条書き・コードブロックなど） |
| スピーカーノート | `<!-- ノート内容 -->` （HTMLコメント形式） |
| フロントマター | 冒頭の `---\nmarp: true\n---` ブロック（省略可） |

> **後方互換**: 旧フォーマットの `<!-- speaker_note -->` セパレーターも引き続き使用できます。

## 💡 ChatGPT / Claude 用プロンプトテンプレート

AIに「このツール向けのMarkdown」を出力してもらうには、以下のプロンプトをコピーして使ってください。

<details>
<summary>📋 プロンプトをクリップボードにコピー（クリックで展開）</summary>

```
あなたは優秀なプレゼンテーション資料作成アシスタントです。
以下のテーマでGoogleスライド用のMarkdownを作成してください。

【テーマ】
（ここにプレゼンのテーマや内容を入力してください）

【出力ルール】
1. 出力はMarkdown形式のみにしてください（説明文は不要）。
2. ファイルの冒頭は必ず以下のフロントマターから始めてください：
   ---
   marp: true
   ---
3. スライドの区切りは `---`（前後に空行）を使ってください。
4. 各スライドのタイトルは `# タイトル` 形式で書いてください。
5. スピーカーノート（話す内容のメモ）は、各スライドの末尾にHTMLコメント形式で書いてください：
   <!-- ここにスピーカーノートを書く -->
6. 全体で5〜8枚程度のスライド構成にしてください。
```

</details>

このプロンプトに「テーマ」を入力してChatGPTやClaudeに送ると、このツールにそのまま貼り付けられる形式のMarkdownが出力されます。

## 💡 仕様に関する重要事項（追記仕様）
このツールは、「既存ファイルの一番最後のページに新しいスライドを追加（Append）」していく安全仕様になっています。
* 生成を実行しても、元々作られていた既存のスライドが上書きされたり消去されることはありません。
* 研修資料などの場合、Day 1のテキストを流し込んだ後、そのまま続けてDay 2のテキストを流し込むことで、1つのファイルの中に全日程分をどんどん継ぎ足していく（追記する）ことが可能です。
* 間違えて2回実行してしまった場合は、末尾に複製された不要なスライドを選択してDeleteキーで削除してください。
